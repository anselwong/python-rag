# 后端开发规范

## 项目背景

本目录是 RAG 知识库项目的 Python 后端。项目计划在两周内完成，用于面试演示和技术讲解。

项目开发者熟悉 Vue 前端，但正在学习 Python、FastAPI 和 RAG。因此，本目录中的代码必须同时满足两个目标：

1. 代码结构清晰、可以运行，适合面试现场演示。
2. 能够通过代码和注释理解关键的 Python、后端与 RAG 技术原理。

优先选择小而明确的实现，不要过早引入复杂框架。除非当前任务明确要求，否则不要使用 LangChain、LangGraph、Agent 框架或微服务架构。

## 语言规范

- 变量、函数、类、API 字段、数据库字段和文件名使用英文。
- 教学注释、技术说明和 README 内容优先使用简洁中文。
- 完成功能后，用中文解释请求流程和关键技术点。
- 默认读者懂前端，但不熟悉 Python 后端。必要时可以使用前端概念帮助理解，例如：Pydantic 模型类似 TypeScript 接口加运行时数据校验。

## 代码注释规范

开发新功能时应添加有价值的注释，尤其要解释与面试相关的关键技术点。

注释应该回答以下问题之一：

- 为什么需要这段代码？
- 它解决了什么问题或边界情况？
- 底层技术机制是什么？
- 为什么选择当前方案，而不是其他方案？
- 修改某个参数会对质量、性能、延迟或成本产生什么影响？

以下位置需要添加注释或文档字符串：

- 职责明确的模块应添加简短的模块说明。
- 公共服务函数和非简单类应添加 docstring。
- docstring 应说明用途、重要参数、返回值和可能抛出的异常。
- 非显而易见的算法或生命周期操作前，应添加简短中文注释。
- 元数据、原始文件、向量数据和模型服务之间的边界需要解释清楚。

以下关键技术第一次出现在代码中时，必须说明其工作原理和作用：

- FastAPI 依赖注入、请求校验、异步处理和 SSE 流式响应。
- 数据库事务、表关系、索引、幂等性和回滚。
- 文档解析、元数据保留、Token 计算、切片大小和重叠切片。
- Embedding 维度、批量处理、向量归一化和模型一致性。
- 向量相似度、Top K、相似度阈值、元数据过滤和向量删除。
- 检索、上下文组装、Token 预算、Prompt、引用和证据不足拒答。
- BM25、混合检索、结果融合、重排序及其效果与延迟权衡。
- Recall@K、MRR、忠实度和答案相关性等评测指标。

不要添加只是翻译代码语法的注释，例如：

```python
# 设置用户名
username = request.username

# 遍历所有文档
for document in documents:
    ...

# 返回结果
return result
```

不要为了增加注释数量而逐行注释。注释如果过时或与代码行为不一致，比没有注释更危险。修改行为时必须同步更新附近的注释。

## 实现规范

- 在项目明确升级之前，保持兼容 Python 3.9。
- 函数参数、返回值和重要变量应使用类型标注。
- HTTP 接口边界必须使用 Pydantic 模型，不要直接传递未经校验的字典。
- API 路由层保持简单，只负责参数接收、依赖注入和响应转换。
- 文档解析、向量化、检索和 RAG 编排放在职责明确的 service 模块中。
- 引入持久化后，数据库访问逻辑与业务逻辑应适当分离。
- 优先编写明确的领域函数，不要过早创建通用抽象。
- 大模型和 Embedding 服务必须能够通过环境变量配置。
- 禁止提交 API Key、`.env`、用户上传文档、数据库文件和向量数据。
- 必须保留引用定位和安全删除所需的文档与切片元数据。

## API 规范

- 应用接口统一放在 `/api/v1` 路径下。
- 资源路径使用名词，CRUD 操作使用标准 HTTP 方法。
- 每个接口明确声明请求模型和响应模型。
- 错误信息应具有可操作性，但不能泄露密钥、内部路径或完整堆栈。
- 知识库、文档、切片和对话必须使用稳定且唯一的 ID。
- 文档导入和删除操作应尽可能保持幂等。
- 新增或修改接口时，同步更新 OpenAPI 的 summary 和说明。

## 测试和学习说明

每个完成的后端功能都应添加与风险相匹配的测试。至少覆盖：

- 一条主要成功路径。
- 一个有意义的失败场景或边界条件。

RAG 算法还需要验证元数据是否完整保留，并优先测试不需要调用付费模型 API 的确定性逻辑。

引入重要技术点时，需要同时完成：

1. 在实现附近添加针对关键机制的中文注释。
2. 如果配置或启动方式发生变化，更新项目 README。
3. 最终交付时解释完整数据流、主要技术取舍，以及两到三个应该掌握的面试问题。

## 前后端契约

修改后端接口时，必须检查前端服务定义是否仍然匹配：

```text
../vue-rag/src/services/
```

接口路径、HTTP 方法、请求字段、响应字段和错误状态必须保持一致。

## 完成前验证

依赖可用时，完成后端修改前至少运行：

```bash
.venv/bin/python -m pytest
.venv/bin/python -m compileall -q app tests
```

如果无法运行测试，交付时必须明确说明原因和未验证的风险。
